package com.hermes.wearos.presentation.theme

import androidx.compose.ui.graphics.Color

// Design System Colors (OLED Pure Black for Wear OS)
object HermesColors {
    // Primary
    val Primary = Color(0xFF0284C7)
    val PrimaryVariant = Color(0xFF0369A1)
    val PrimaryLight = Color(0xFF38BDF8)

    // Semantic
    val Success = Color(0xFF059669)
    val Warning = Color(0xFFF59E0B)
    val Error = Color(0xFFDC2626)

    // Background / Surface (OLED Pure Black)
    val Background = Color(0xFF000000)
    val Surface = Color(0xFF121212)
    val SurfaceVariant = Color(0xFF1E1E1E)

    // Text
    val OnPrimary = Color(0xFFFFFFFF)
    val OnBackground = Color(0xFFFFFFFF)
    val OnSurface = Color(0xFFE2E8F0)
    val OnSurfaceVariant = Color(0xFF94A3B8)

    // Status
    val StatusOnline = Color(0xFF22C55E)
    val StatusOffline = Color(0xFFEF4444)
    val StatusWarning = Color(0xFFFBBF24)
}
